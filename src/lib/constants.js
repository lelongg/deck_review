// Storage keys shared across the app.
export const TOKEN_KEY = 'deck:token'
export const WRAP_KEY = 'deck:wrap'
// Last opened PR { owner, repo, number } — lets a reload resume where you were.
export const SESSION_KEY = 'deck:session'
